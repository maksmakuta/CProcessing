cmake_minimum_required(VERSION 3.20 FATAL_ERROR)
project(sketch CXX)
add_subdirectory(libprocessing)
add_executable(sketch sketch.cpp)
include_directories(libprocessing/src)
target_link_libraries(sketch PRIVATE processing)
